import { CustomNgbPaginationDirective } from './custom-ngb-pagination.directive';

describe('CustomNgbPaginationDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNgbPaginationDirective();
    expect(directive).toBeTruthy();
  });
});
